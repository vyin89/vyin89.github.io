import { Experience } from "./Experience";

export type SelectedData = {
  title: string;
  tags: string[];
  company: string;
  start: string;
  end: string;
  achievements: string[];
};

export default Experience;
